server:
 port: 8888

#--- config server configurations ---#
spring:
  application:
   name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/LindoManana/spring-cloud-configurations
          searchPaths: '{application}'